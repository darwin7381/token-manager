# Dashboard 優化與使用追蹤完整配置

**完成時間**: 2025-11-05  
**版本**: v2.7  
**狀態**: ✅ 完成並優化

---

## 🎨 Dashboard 設計優化

### **問題修復**

1. ✅ **顏色系統統一**：
   - 移除硬編碼顏色（如 `#111827`, `#6b7280`）
   - 改用 CSS 變數（`var(--text-primary)`, `var(--bg-primary)`）
   - 完美支持暗夜模式

2. ✅ **按鈕樣式修復**：
   - 右上角刷新按鈕使用系統 `.btn-secondary` 樣式
   - 所有按鈕統一使用設計系統

3. ✅ **設計風格統一**：
   - 卡片邊框使用 `var(--border-color)`
   - 陰影使用 `var(--shadow-sm/md/lg)`
   - 背景使用 `var(--bg-primary/secondary/tertiary)`

### **CSS 變數對照表**

```css
/* Light Theme */
--bg-primary: #ffffff
--bg-secondary: #f8fafc
--bg-tertiary: #f1f5f9
--text-primary: #0f172a
--text-secondary: #64748b
--text-tertiary: #94a3b8
--border-color: #e2e8f0
--accent-primary: #3b82f6
--accent-success: #10b981
--accent-warning: #f59e0b
--accent-danger: #ef4444

/* Dark Theme */
--bg-primary: #0f172a
--bg-secondary: #1e293b
--bg-tertiary: #334155
--text-primary: #f1f5f9
--text-secondary: #cbd5e1
--text-tertiary: #64748b
--border-color: #334155
```

### **優化的組件**

#### **Dashboard.css**
- ✅ 所有顏色改用 CSS 變數
- ✅ 統計卡片背景透明化
- ✅ 圖標背景使用 alpha 通道
- ✅ 暗夜模式完美支持

#### **SystemHealth.css**
- ✅ 所有顏色改用 CSS 變數
- ✅ 卡片樣式統一
- ✅ 狀態顏色保持（健康/警告/錯誤）

#### **AuditLogs.css**
- ✅ 表格樣式統一
- ✅ 篩選器樣式優化
- ✅ 完美暗夜模式支持

---

## 🔧 Token 使用追蹤配置

### **架構說明**

```
n8n Workflow
    ↓ (X-API-Key: ntk_xxx)
Cloudflare Worker
    ├─→ 驗證 Token ✅
    ├─→ 轉發到後端微服務 ✅
    └─→ ctx.waitUntil(記錄使用) ← 異步，不阻塞
           ↓
        POST https://tapi.blocktempo.ai/api/usage-log
           ↓
        更新 Token 的 last_used 時間
```

**重點**：
- ✅ 異步執行，不影響 API 性能
- ✅ 失敗不影響主流程
- ✅ 5 秒超時保護

---

## 🌐 環境配置

### **1. Worker 配置（wrangler.toml）**

```toml
# 生產環境
[vars]
TOKEN_MANAGER_BACKEND = "https://tapi.blocktempo.ai"

# 本地開發環境
[env.dev]
vars = { TOKEN_MANAGER_BACKEND = "http://localhost:8000" }
```

### **2. 本地開發測試**

使用本地環境變數：
```bash
cd worker

# 本地運行（自動使用 env.dev 配置）
npm run dev
# 或
wrangler dev

# 會使用 http://localhost:8000 作為後端
```

### **3. 生產環境部署**

```bash
cd worker

# 部署到生產環境
npm run deploy
# 或
wrangler deploy

# 會使用 https://tapi.blocktempo.ai 作為後端
```

---

## 🧪 本地測試指南

### **方法 1：使用測試腳本**

```bash
# 1. 給予執行權限（已完成）
chmod +x test_usage_tracking.sh

# 2. 基礎測試
./test_usage_tracking.sh

# 3. 使用真實 token_hash 測試
./test_usage_tracking.sh <your_token_hash>
```

### **方法 2：手動測試**

#### **步驟 1：獲取真實 token_hash**

```bash
# 方式 A: 查詢資料庫
psql $DATABASE_URL -c "SELECT token_hash, name FROM tokens LIMIT 1;"

# 方式 B: 從前端創建 Token
# 1. 前往 Token 管理頁面
# 2. 創建一個測試 Token
# 3. Token 創建後，查詢資料庫獲取 hash
```

#### **步驟 2：發送測試請求**

```bash
# 替換 YOUR_TOKEN_HASH 為實際值
curl -X POST http://localhost:8000/api/usage-log \
  -H "Content-Type: application/json" \
  -d '{
    "token_hash": "YOUR_TOKEN_HASH",
    "route": "/api/test",
    "timestamp": 1699000000000
  }'

# 預期返回：{"status":"logged"}
```

#### **步驟 3：驗證更新**

```bash
# 查詢 Token 的 last_used 時間
psql $DATABASE_URL -c "SELECT name, last_used FROM tokens WHERE token_hash = 'YOUR_TOKEN_HASH';"

# 或者在前端刷新 Token 列表，查看「最後使用」欄位
```

---

## 🔍 Worker 本地測試

### **啟動本地 Worker**

```bash
cd worker

# 方式 1：使用 npm script
npm run dev

# 方式 2：直接使用 wrangler
wrangler dev

# 預期輸出：
# ⛅️ wrangler 3.x.x
# ------------------
# Your worker has access to the following bindings:
# - KV Namespaces:
#   - TOKENS: c36cc6c8cc38473dad537a0ab016d83f
# - Vars:
#   - TOKEN_MANAGER_BACKEND: "http://localhost:8000"
```

### **測試 Worker 調用**

```bash
# 1. 創建測試 Token（在前端）
# 2. 複製 Token（例如：ntk_abc123...）
# 3. 通過 Worker 調用 API

curl -X POST http://localhost:8787/api/test \
  -H "X-API-Key: ntk_YOUR_TEST_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}'

# 預期：
# 1. Worker 驗證 Token ✅
# 2. Worker 轉發請求 ✅
# 3. Worker 異步記錄使用 ✅
```

### **驗證使用記錄**

```bash
# 方式 1：查看後端日誌
# 應該看到：POST /api/usage-log {"status":"logged"}

# 方式 2：查詢資料庫
psql $DATABASE_URL -c "
  SELECT name, created_at, last_used 
  FROM tokens 
  WHERE name = 'YOUR_TEST_TOKEN_NAME';
"

# 方式 3：前端查看
# 刷新 Token 列表，查看「最後使用」時間
```

---

## 📊 監控數據整合

### **當前實現**

Token 列表已顯示 `last_used` 時間：
- 有使用：顯示時間（如：2025-11-05 10:30）
- 未使用：顯示「尚未使用」

### **未來可整合到 Dashboard**

#### **1. 使用統計卡片**
```jsx
<StatCard
  icon={<Activity />}
  title="本週使用次數"
  value={weeklyUsageCount}
  color="purple"
  trend="+15% vs 上週"
/>
```

#### **2. Token 使用趨勢圖**
```jsx
// 每小時的 Token 使用量
<LineChart data={hourlyUsageData}>
  <Line dataKey="count" name="API 調用次數" />
</LineChart>
```

#### **3. 最活躍 Token Top 10**
```jsx
<BarChart data={topTokens}>
  <Bar dataKey="usage_count" name="使用次數" />
</BarChart>
```

#### **4. 路由使用分佈**
```jsx
<PieChart>
  <Pie data={routeUsage} dataKey="count" nameKey="route" />
</PieChart>
```

### **需要的數據表（未來擴展）**

如果要實現詳細的使用統計，需要創建：

```sql
CREATE TABLE token_usage_logs (
    id SERIAL PRIMARY KEY,
    token_hash VARCHAR(64) NOT NULL,
    route_path VARCHAR(255),
    used_at TIMESTAMP NOT NULL DEFAULT NOW(),
    
    -- 可選：更詳細的資訊
    response_status INTEGER,
    response_time_ms INTEGER,
    ip_address VARCHAR(45),
    user_agent TEXT
);

CREATE INDEX idx_usage_token_hash ON token_usage_logs(token_hash);
CREATE INDEX idx_usage_used_at ON token_usage_logs(used_at DESC);
CREATE INDEX idx_usage_route ON token_usage_logs(route_path);
```

目前只更新 `last_used` 時間，不創建詳細日誌表（節省空間）。

---

## 🚀 部署配置

### **Railway 後端部署**

未來部署時需要配置自定義域名：

```yaml
# Railway 設定
Service: token-manager-backend
Custom Domain: tapi.blocktempo.ai

# 確保 CORS 允許此域名
# backend/main.py 中的 CORS 配置已設為 allow_origins=["*"]
```

### **Cloudflare Worker 部署**

```bash
cd worker

# 部署（會使用 wrangler.toml 中的生產配置）
wrangler deploy

# 檢查環境變數
wrangler tail

# 應該看到：
# TOKEN_MANAGER_BACKEND = "https://tapi.blocktempo.ai"
```

---

## 📝 測試檢查清單

### **Dashboard 優化測試**
- [ ] 明亮模式：所有文字清晰可讀
- [ ] 暗夜模式：所有文字清晰可讀
- [ ] 統計卡片顏色正確（藍/綠/紫/橙）
- [ ] 圖表渲染正常
- [ ] 刷新按鈕樣式正確
- [ ] 卡片懸停效果正常
- [ ] 響應式設計（手機/平板）

### **系統健康監控測試**
- [ ] 整體狀態顯示正確
- [ ] 數據庫狀態：正常（綠色）
- [ ] Cloudflare KV 狀態：正常（綠色）
- [ ] Clerk 狀態：正常（綠色）
- [ ] 立即檢查按鈕正常
- [ ] 自動刷新（30 秒）正常
- [ ] 暗夜模式下所有文字可讀

### **審計日誌測試**
- [ ] 日誌列表顯示正常
- [ ] 篩選功能正常
- [ ] 分頁功能正常
- [ ] CSV 匯出正常
- [ ] 暗夜模式表格可讀

### **使用追蹤測試**
- [ ] 本地後端 API 測試通過
- [ ] 測試腳本執行成功
- [ ] Token last_used 時間更新
- [ ] Worker 本地測試（wrangler dev）
- [ ] Worker 生產測試（wrangler deploy）

---

## 🔧 故障排查

### **問題：Dashboard 圖表不顯示**

**可能原因**：
1. recharts 未安裝
2. 數據格式不正確
3. CSS 變數未定義

**解決方案**：
```bash
# 確認依賴已安裝
cd frontend
npm list recharts date-fns

# 檢查瀏覽器 Console 是否有錯誤
# 檢查 Network tab 查看 API 返回數據
```

### **問題：使用追蹤不工作**

**可能原因**：
1. Worker 環境變數未設置
2. 後端 URL 不可達
3. CORS 問題

**解決方案**：
```bash
# 檢查 Worker 環境變數
wrangler dev --log-level debug

# 檢查後端是否可訪問
curl -X POST https://tapi.blocktempo.ai/api/usage-log \
  -H "Content-Type: application/json" \
  -d '{"token_hash":"test","route":"/test","timestamp":123}'

# 檢查 Worker 日誌
wrangler tail
```

### **問題：暗夜模式下看不清文字**

**檢查**：
- 確認使用了 `var(--text-primary)` 而非硬編碼顏色
- 確認 `[data-theme='dark']` 選擇器正確設置

---

## 📈 數據流向圖

### **管理操作（已有）**
```
用戶操作前端
    ↓
Token Manager Backend
    ↓
寫入 PostgreSQL
    ↓
審計日誌記錄
    ↓
Dashboard 顯示
```

### **Token 使用（新增）**
```
n8n Workflow
    ↓ X-API-Key
Cloudflare Worker
    ├─→ 驗證 Token（從 KV）
    ├─→ 轉發請求（到後端微服務）
    └─→ 異步記錄（到 Token Manager）
           ↓
        POST /api/usage-log
           ↓
        UPDATE tokens SET last_used = NOW()
           ↓
        Token 列表顯示「最後使用」
           ↓
        未來：Dashboard 使用統計圖表
```

---

## 🎯 下一步建議

### **立即測試**
1. 重啟 Vite（刷新頁面查看優化效果）
2. 測試暗夜模式（點擊右上角主題切換）
3. 執行使用追蹤測試腳本

### **短期（可選）**
1. 添加詳細使用日誌表
2. Dashboard 顯示使用統計
3. 識別長時間未使用的 Token

### **中期（可選）**
1. 使用趨勢分析
2. 路由熱度地圖
3. 異常使用檢測

---

## 📊 預期效果展示

### **Dashboard 頁面**
```
┌─────────────────────────────────────────┐
│ 📊 系統 Dashboard      [刷新數據]      │
│ 即時監控系統狀態與活動                  │
├─────────────────────────────────────────┤
│ [活躍Token] [路由總數] [團隊] [過期]   │
│    16         2         7       0       │
├─────────────────────────────────────────┤
│ 📈 Token創建趨勢  │  📊 團隊分佈      │
│  (折線圖)          │  (柱狀圖)         │
├─────────────────────────────────────────┤
│ ⚠️ 即將過期的 Token (如果有)           │
├─────────────────────────────────────────┤
│ 🕒 最近活動                             │
│ [更新] Token "xxx" - 11/04 09:47       │
└─────────────────────────────────────────┘
```

### **系統健康監控**
```
┌─────────────────────────────────────────┐
│ 🏥 系統健康監控      [立即檢查]        │
├─────────────────────────────────────────┤
│      系統狀態                           │
│       ✓ 正常                            │
│   版本: 1.0.0 | 服務: token-manager    │
├─────────────────────────────────────────┤
│ [數據庫✓] [Cloudflare KV✓] [Clerk✓]   │
├─────────────────────────────────────────┤
│ 系統資訊                                │
│ 檢查時間: 2025/11/5 下午5:46          │
└─────────────────────────────────────────┘
```

---

## ✅ 完成狀態

### **設計優化**
- ✅ CSS 變數統一使用
- ✅ 暗夜模式完美支持
- ✅ 按鈕樣式修復
- ✅ 設計風格統一

### **使用追蹤**
- ✅ 後端 API 實現
- ✅ Worker 異步記錄
- ✅ 環境變數配置
- ✅ 本地/生產分離
- ✅ 測試腳本提供

### **文檔**
- ✅ 配置文檔
- ✅ 測試指南
- ✅ 故障排查
- ✅ 未來擴展建議

---

## 🎉 總結

### **優化內容**
1. ✅ Dashboard 設計系統統一
2. ✅ 暗夜模式完美支持
3. ✅ 按鈕樣式修復
4. ✅ Token 使用追蹤實現
5. ✅ 環境變數配置（tapi.blocktempo.ai）
6. ✅ 本地測試腳本
7. ✅ 完整文檔

### **技術亮點**
- 🎨 完全使用設計系統 CSS 變數
- ⚡ Worker 異步記錄，零性能影響
- 🌐 環境分離（本地/生產）
- 🧪 完整測試工具
- 📚 詳細文檔

**現在系統設計更統一、更專業、功能更完整！** 🚀

---

**文件版本**: 1.0  
**最後更新**: 2025-11-05  
**狀態**: 完成

